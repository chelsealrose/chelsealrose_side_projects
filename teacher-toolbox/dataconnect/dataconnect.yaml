specVersion: "v1"
serviceId: "teacher-toolbox"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "teacher-toolbox-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
